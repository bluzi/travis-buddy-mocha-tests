const assert = require('assert');

describe('one', () => {
    it('should be equal to two', () => {
        assert(1 === 2);
    });

    it('should be greather than zero', () => {
        assert(1 > 0);
    })
});